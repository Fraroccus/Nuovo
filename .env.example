# Copy this file to .env for local development
# Managed Postgres connection string (Neon, Supabase, Railway, etc.)
# Use sslmode=require on providers that require SSL
# Example (Neon): postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/dbname?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&sslmode=require"

# Optional: A direct (non-pooled) connection string for migrations and Prisma
# If your provider gives you a pooled URL and a direct URL, set DIRECT_URL to the direct one.
# Example (Neon): postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/dbname?sslmode=require
DIRECT_URL=""
